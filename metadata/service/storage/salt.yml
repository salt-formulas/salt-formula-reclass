applications:
- reclass
classes:
- service.reclass.support
parameters:
  reclass:
    storage:
      enabled: true
      base_dir: /srv/salt/reclass
      nodes_uri:
      output: yaml
      data_source:
        engine: git
        address: ${_param:reclass_data_repository}
        branch: ${_param:reclass_data_revision}
